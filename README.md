# for-yash

A simple one-page site: story, gallery, games, and a letter.

**Edit content:** `config.js` (names, quiz, gallery images, letter text).  
**Edit copy:** `index.html` (hero, story, footer).

**Deploy:** Push to `main`. Enable GitHub Pages in repo **Settings → Pages** (source: branch `main`, folder `/ (root)`).  
Live at: `https://shambhavisingh97.github.io/for-yash/`
